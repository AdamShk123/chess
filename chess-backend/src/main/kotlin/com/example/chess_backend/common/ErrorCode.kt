package com.example.chess_backend.common

enum class ErrorCode {
    DUPLICATE_NAME,
    DUPLICATE_EMAIL,
    USER_ALREADY_REGISTERED,
    USER_NOT_FOUND,
    MATCH_NOT_FOUND,
    VALIDATION_ERROR,
    FORBIDDEN
}